# herdr configuration — managed by home-manager (modules/shared/programming/ai.nix)
# Full reference: `herdr --default-config`

[keys]
# Match the tmux prefix (see modules/shared/tmux.nix).
prefix = "ctrl+s"
